campaign: {name: haifa-smoke, timezone: Asia/Jerusalem, duration_days: 1, windows_local: ["12:00"]}
exchanges: {priority: [binance, bybit, okx, bitget, gate, mexc, kucoin, kraken, bingx, phemex]}
symbols: {major: [BTCUSDT, ETHUSDT, SOLUSDT]}
probes: {iterations: 1, timeout_seconds: 8, websocket_observation_seconds: 3, bounded_concurrency: 3, per_exchange_concurrency: 1, jitter_ms: 50}
scoring:
  profile: manual-futures-scalping
  weights: {websocket: 0.25, rest_p95: 0.20, tail_stability: 0.15, reliability: 0.10, freshness: 0.10, market_quality: 0.10, market_breadth: 0.05, accessibility: 0.05}
storage_path: data/cexlatency.db
report_directory: reports/generated

